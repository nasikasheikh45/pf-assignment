#include<iostream>
using namespace std;
int main(){
	double a=10,b=10;
	a=a+0.2;
	if(a>b)
	 cout<<"a is the larger";
	 else if (b>a)
	 cout<<"b is larger ";
	 else
	 cout<<"both are equal";
	 return 0;
}
